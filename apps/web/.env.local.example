NEXT_PUBLIC_API_URL=http://localhost:8000
# Used only by Next.js Server Components and the same-origin /backend proxy.
# In Docker Compose this normally resolves to the API service name.
API_URL_INTERNAL=http://api:8000

# Wave 4 Roadmap — 50 capabilities

**Status: 50 / 50 complete.** Every G01–G50 item is `done` with its named acceptance evidence
present in the worktree. Same rules as `FEATURE_LEDGER.md`: an item is `done` only when its
implementation **and** its acceptance test/artifact both exist. Delivered across nine themed batches,
each verified (backend pytest + frontend vitest + ruff + tsc + lint + build + Alembic single-head
upgrade/check) and CI-green before the next began. No new runtime dependencies were added — every
capability is built on the existing stack (the hash-pinned `requirements.lock` is unchanged).

**Design constraints carried forward from Waves 1–3** (non-negotiable):
keyless-by-default data sources; deterministic outputs unless a human explicitly enables the LLM;
never impute missing data; explicit source status instead of false-clean empties; append-only
governed records; every material claim cites resolvable evidence.

Carryovers from the Wave 3 ledger: F41 → G17, F55 → G18.

---

## Theme A — Retrieval & grounded AI (10)

The deepest differentiator for AI-role interviews: real hybrid retrieval, measured with evals,
and generation that is provably constrained by evidence.

| ID | Capability | Effort | Acceptance evidence |
|---|---|---|---|
| G01 | Hybrid retrieval: pgvector-ready embeddings fused with BM25 via reciprocal-rank fusion; deterministic local keyless embedding (feature-hashing, no ML deps) so the default stays keyless | L | `done` — fusion-ranking + BM25-contract tests (`apps/api/tests/test_hybrid_retrieval.py`: `test_rrf_ranks_a_chunk_both_rankers_like_above_a_single_ranker_favorite`, `test_hybrid_returns_fused_results_when_embeddings_exist`, `test_retrieve_bm25_signature_and_return_shape_unchanged`, `test_hybrid_falls_back_to_bm25_when_no_embeddings`) |
| G02 | Embedding ingestion pipeline: chunk embeddings persisted at ingest (`DocumentChunk.embedding` vector + `embedding_id` method tag wired), plus a backfill worker for existing workspaces | M | `done` — ingest + backfill idempotency tests (`apps/api/tests/test_hybrid_retrieval.py`: `test_embed_is_deterministic_and_l2_normalized`, `test_cosine_high_for_identical_low_for_unrelated`, `test_empty_text_is_zero_vector_and_cosine_zero`, `test_ingest_persists_embeddings`, `test_backfill_fills_only_nulls_and_is_idempotent`) |
| G03 | Retrieval evaluation harness: golden question set over fixture filings; recall@k and MRR computed in CI and tracked in a committed metrics file | M | `done` — committed golden set (`src/eval/fixtures/golden_set.json`) + baseline (`src/eval/retrieval_metrics.json`); CI regression gate (`apps/api/tests/test_retrieval_eval.py`: `test_eval_computes_recall_and_mrr_for_every_ranker`, `test_metrics_meet_absolute_quality_floors`, `test_no_regression_below_committed_baseline`, `test_hybrid_never_underperforms_bm25`) |
| G04 | Grounded synthesis mode (live LLM): fluent answers composed **only** from retrieved extracts, gated by the citation auditor, abstention preserved | L | `done` — adversarial fail-closed tests (`apps/api/tests/test_grounded_synthesis.py`: `test_faithful_rewrite_is_applied_and_records_manifest`, `test_fabricated_number_is_rejected_and_extractive_answer_is_served`, `test_fabricated_citation_is_rejected_and_extractive_answer_is_served`, `test_empty_rewrite_falls_back_to_extractive`, `test_abstention_is_preserved_and_llm_never_called`, `test_no_consent_stays_extractive`, `test_mock_mode_stays_extractive`, `test_ask_default_is_purely_extractive`, `test_ask_grounded_without_consent_stays_extractive`) |
| G05 | LLM-as-judge faithfulness evals with persisted eval runs and a quality dashboard per model/prompt version | L | `done` — mock-judge + persistence/quality-view tests (`apps/api/tests/test_judge_evals.py`: `test_mock_judge_passes_a_grounded_answer`, `test_mock_judge_flags_an_unsupported_number`, `test_mock_judge_flags_an_unsupported_citation`, `test_answer_with_no_checkable_claims_is_trivially_faithful`, `test_persisted_judgment_roundtrips_with_provenance`, `test_quality_summary_groups_by_model_and_prompt`) |
| G06 | Abstention calibration: score distributions for answered vs abstained questions; thresholds justified by a committed calibration study | M | `done` — committed study (`src/eval/calibration_study.md`) + runner (`src/eval/calibration.py`); boundary + drift-guard tests (`apps/api/tests/test_calibration.py`: `test_service_threshold_matches_calibrated_value`, `test_coverage_at_threshold_is_answered`, `test_coverage_just_below_threshold_flips_to_partial`, `test_no_overlap_question_abstains`, `test_calibration_classes_are_separable_at_the_threshold`) |
| G07 | Cross-year 10-K semantic diff: risk-factor drift (added / removed / materially changed) with citations into both filings | L | `done` — embedding-alignment drift classification on fixture year-pairs (`apps/api/tests/test_filing_diff.py`: `test_risk_diff_classifies_added_removed_and_changed`, `test_risk_diff_endpoint_contract`, `test_single_filing_is_unavailable_not_fabricated`) |
| G08 | Unified cross-corpus Q&A: one question over filings + data-room docs with provenance-aware citations (public vs confidential clearly labeled) | M | `done` — mixed-corpus citation labeling + abstention (`apps/api/tests/test_cross_corpus_qa.py`: `test_answer_cites_both_corpora_with_confidentiality_labels`, `test_filings_only_workspace_answers_labeled_public`, `test_abstains_when_neither_corpus_matches`, `test_cross_corpus_endpoint_contract`) |
| G09 | Embedding-similarity comp discovery from business descriptions, shown side-by-side with the SIC-code method and its disagreements | M | `done` — similarity-ranking + disagreement + determinism + endpoint-contract tests (`apps/api/tests/test_comp_similarity.py`: `test_similarity_orders_peers_by_closeness_to_target`, `test_disagreements_surface_embedding_only_and_sic_only`, `test_peer_in_both_sets_is_not_a_disagreement`, `test_missing_target_description_is_unavailable_not_fabricated`, `test_no_peer_descriptions_is_unavailable`, `test_undescribed_peer_is_excluded_not_scored_zero`, `test_ranking_is_deterministic`, `test_equal_similarity_ties_break_by_ticker`, `test_similarity_endpoint_returns_comparison_contract`) |
| G10 | Prompt & model-config registry: versioned, hashed prompt manifests bound to every LLM-touched artifact run (reproducible LLM ops) | M | `done` — hash round-trip + tamper-detection + run-binding tests (`apps/api/tests/test_prompt_registry.py`: `test_manifest_hash_is_the_sha256_of_the_registered_template`, `test_grounded_synthesis_prompt_is_registered_and_hashed`, `test_changing_the_template_text_changes_the_hash`, `test_prompt_manifest_binds_into_a_sealed_llm_run`, `test_deterministic_run_has_no_prompt_manifest`, `test_prompt_manifest_endpoint_lists_registered_prompts`) |

## Theme B — Public-data research depth (10)

Widens the moat of "real data, no keys" — the research-analyst credibility layer.

| ID | Capability | Effort | Acceptance evidence |
|---|---|---|---|
| G11 | 10-Q quarterly ingestion + trailing-twelve-month metric derivation | M | `done` — TTM arithmetic tests across fiscal-year boundaries (`apps/api/tests/test_quarterly_ttm.py`) |
| G12 | XBRL segment-level revenue (dimensional facts) with segment trend charts | L | `done` — dimensional-member extraction, consolidated-only "unavailable" (never fabricated), reconciliation-based `partial`, trend ordering + endpoint contract (`apps/api/tests/test_segments.py`) |
| G13 | DEF 14A proxy ingestion: executive compensation table + governance red flags (staggered board, dual-class) | L | `done` — Summary Compensation Table parse into NEO rows with missing-value-stays-`None` (never imputed), governance red-flag heuristics (staggered/classified board fires on "classified board"/"three classes" and stays quiet on clean text, dual-class super-voting, combined CEO/Chair, poison pill), EDGAR-outage → `unavailable` (never false-clean), and store+get endpoint roundtrip (`apps/api/tests/test_proxy_governance.py`) |
| G14 | 13F institutional ownership snapshot + holder-concentration analysis | M | `done` — parse + concentration math tests (`apps/api/tests/test_ownership.py`: `test_parse_13f_infotable_reads_all_positions`, `test_parse_13f_infotable_malformed_returns_empty`, `test_concentration_math_is_hand_verified`, `test_concentration_is_scale_invariant_and_single_holding_is_one`, `test_concentration_excludes_missing_and_nonpositive_values_never_imputes`, `test_concentration_empty_is_none_not_zero`, `test_parse_and_concentration_compose_end_to_end`, `test_institutional_ownership_outage_is_unavailable_not_clean_empty`, `test_non_manager_target_is_not_applicable_with_honest_note`, `test_manager_target_reports_portfolio_concentration`) |
| G15 | 13D/13G activist-stake detection wired into the signals timeline | S | `done` — event classification tests (`apps/api/tests/test_ownership.py`: `test_classify_13d_is_activist_and_13g_is_passive`, `test_classify_marks_amendments_and_tolerates_form_variants`, `test_build_stake_event_preserves_missing_fields_as_none`, `test_cover_page_extraction_of_filer_and_percent`, `test_activist_stakes_outage_is_unavailable_not_clean_empty`, `test_activist_stakes_classifies_submissions_into_timeline`) |
| G16 | Debt maturity schedule extraction from filings + maturity-wall chart | L | `done` — schedule extraction + never-impute gaps + endpoint/legacy + maturity-wall-flag tests (`apps/api/tests/test_debt_maturities.py`: `test_all_buckets_tagged_yields_full_ordered_schedule_and_total`, `test_missing_bucket_is_reported_and_never_imputed`, `test_no_maturity_concepts_is_unavailable_not_clean_empty`, `test_partial_status_when_only_some_buckets_present`, `test_only_latest_balance_sheet_date_populates_the_schedule`, `test_amendment_precedence_keeps_latest_filed_value`, `test_debt_maturities_endpoint_contract`, `test_debt_maturities_endpoint_unavailable_before_refresh`, `test_near_term_maturity_wall_flag_fires_when_y1_y2_dominate`, `test_no_maturity_wall_flag_when_back_loaded`, `test_no_maturity_wall_flag_on_partial_schedule`) |
| G17 | Fiscal-period consistency diagnostics (carryover F41): mixed-period operands flagged, never silently blended | M | `done` — mismatch detection tests (`apps/api/tests/test_fiscal_diagnostics.py`) |
| G18 | Consolidated signals overview page (carryover F55): one screen aggregating events/insiders/news/themes with per-source status | S | `done` — per-section source-status aggregation (an unavailable feed shows `unavailable`, never a clean-empty merge; roll-up is honest) + endpoint contract (`apps/api/tests/test_signals_overview.py`: `test_overview_aggregates_four_sections_each_with_source_status`, `test_unavailable_feed_shows_unavailable_not_clean_empty`, `test_overall_status_is_unavailable_only_when_every_feed_is_down`, `test_no_target_raises_not_found`, `test_signals_overview_endpoint_contract`, `test_signals_overview_missing_workspace_404`) |
| G19 | Watchlists with scheduled refresh: track N companies, detect new filings, emit notification/webhook events through the existing outbox | M | `done` — add/list/remove, refresh baseline→detect-one→dedup, multi-filing chronological emission, EDGAR-outage safety, audit→notification mapping, cross-org isolation, scheduled worker (`apps/api/tests/test_watchlist.py`: `test_add_list_remove_entry_by_cik`, `test_add_by_ticker_resolves_cik`, `test_add_is_idempotent_and_reactivates`, `test_add_without_identifier_is_rejected`, `test_refresh_baseline_then_detects_one_new_filing_then_dedups`, `test_refresh_emits_multiple_new_filings_oldest_first`, `test_refresh_edgar_outage_emits_nothing_and_marks_unavailable`, `test_emitted_event_maps_to_a_notification`, `test_refresh_is_cross_org_isolated`, `test_refresh_all_covers_every_org_with_active_entries`) |
| G20 | Insider-pattern analytics: clustered buying/selling windows, 10b5-1 plan flags, officer-vs-director splits | M | `done` — clustering + classification tests (`apps/api/tests/test_insider_patterns.py`: `test_adjacent_same_direction_trades_form_one_cluster`, `test_gap_over_threshold_splits_clusters`, `test_buys_and_sells_never_merge_into_one_window`, `test_plan_summary_counts_planned_discretionary_and_unknown`, `test_missing_plan_flag_is_unknown_never_discretionary`, `test_role_split_sums_buys_and_sells_by_role`, `test_unavailable_feed_is_unavailable_not_clean_zero`, `test_parse_form4_flags_10b5_1_from_footnote_and_reads_roles`, `test_parse_form4_document_level_10b5_1_checkbox`, `test_parse_form4_no_10b5_1_indicator_is_unknown`) |

## Theme C — Underwriting & quantitative depth (10)

The PE-domain fluency layer — complex, and exactly what a diligence/valuation interviewer probes.

| ID | Capability | Effort | Acceptance evidence |
|---|---|---|---|
| G21 | Monte Carlo LBO: driver distributions, percentile IRR/MoIC bands, deterministic seeding so runs are reproducible | L | **done** — `tests/test_monte_carlo_attribution.py::test_same_seed_is_byte_identical_and_different_seed_moves_the_median`, `::test_monte_carlo_percentiles_are_ordered_and_iterations_are_accounted_for`, `::test_zero_variance_distributions_collapse_to_the_deterministic_result`, `::test_monte_carlo_validation_rejects_bad_iterations_and_unknown_drivers` |
| G22 | Returns attribution bridge: entry/exit multiple vs deleveraging vs EBITDA growth decomposition, reconciling exactly to total return | M | **done** — `tests/test_monte_carlo_attribution.py::test_attribution_components_sum_exactly_to_total_value_creation`, `::test_attribution_endpoint_reconciles` |
| G23 | Covenant headroom projection: quarter-by-quarter headroom under each case with breach-quarter detection | M | **done** — `tests/test_underwriting_analytics.py::test_covenant_headroom_breach_boundary_is_the_threshold_crossing_quarter`, `::test_covenant_headroom_endpoint_and_requires_a_covenant`, `::test_covenant_headroom_rejects_invalid_assumptions` |
| G24 | Driver-based operating model: user-defined drivers with formula validation, cycle detection, and provenance on every derived line | L | **done** — whitelisted-AST formula eval (no `eval`/`exec`), DFS cycle path, transitive-input provenance (`tests/test_driver_and_events.py::test_driver_model_resolves_dag_in_topological_order_with_provenance`, `::test_driver_model_rejects_a_direct_cycle_naming_the_path`, `::test_driver_model_rejects_a_self_reference`, `::test_driver_model_rejects_an_unknown_reference`, `::test_driver_model_rejects_unsafe_formulas`, `::test_driver_model_endpoint_contract_and_cycle_is_422`) |
| G25 | Working-capital seasonality modeling from monthly imports (peg by month, not annual average) | M | **done** — per-month peg with peak/trough/amplitude; absent months reported, never imputed (`tests/test_driver_and_events.py::test_seasonality_pegs_every_month_and_reports_peak_trough_amplitude`, `::test_seasonality_averages_repeated_months_and_never_imputes_absent_ones`, `::test_seasonality_endpoint_contract`) |
| G26 | Dividend recap and bolt-on acquisition modeling inside case versions | L | **done** — recap/bolt-on overlaid on the base case with Decimal-exact sources/uses + `xirr`-repriced returns (`tests/test_driver_and_events.py::test_dividend_recap_increases_leverage_and_lifts_irr_with_balanced_sources_uses`, `::test_bolt_on_adds_ebitda_and_returns_reflect_the_accretion`, `::test_equity_funded_bolt_on_adds_a_sponsor_outflow`, `::test_recap_bolton_endpoint_and_unknown_period_is_422`) |
| G27 | Management-vs-sponsor case variance analysis: line-level deltas with materiality ranking | S | **done** — `tests/test_underwriting_analytics.py::test_case_variance_lines_reconcile_and_rank_by_materiality`, `::test_case_variance_endpoint_compares_persisted_cases`, `::test_case_variance_operand_requires_exactly_one_source` |
| G28 | Exit readiness scorecard + hold-period sensitivity (3/5/7-year grids) | M | **done** — `tests/test_underwriting_analytics.py::test_exit_readiness_scorecard_names_thresholds_and_grids_holds`, `::test_exit_readiness_endpoint` |
| G29 | Fund-level portfolio construction: aggregated exposure vs concentration limits, simple pacing model | L | **done** — `tests/test_fund_construction.py::test_exposure_aggregation_sums_to_sized_capital`, `::test_limit_breach_detection_flags_over_concentration_with_excess`, `::test_single_deal_and_strategy_limits`, `::test_pacing_status_tracks_vintage_schedule`, `::test_sizing_coverage_excludes_unsized_deals_without_imputing`, `::test_fund_construction_is_organization_scoped`, `::test_fund_id_filter_narrows_to_a_single_fund` |
| G30 | Valuation football field: triangulation methods on one chart with explicit method weights and excluded-method reasons | S | **done** — `tests/test_underwriting_analytics.py::test_football_field_weights_sum_to_one_and_excluded_methods_carry_reasons`, `::test_football_field_endpoint_and_requires_a_method` |

## Theme D — Platform engineering & scale (10)

The senior-engineer credibility layer: the app already works; this makes it *operable*.

| ID | Capability | Effort | Acceptance evidence |
|---|---|---|---|
| G31 | Durable job queue: workspace builds move from in-process BackgroundTasks to a DB-backed job table + worker with retries, heartbeats, and stale-claim recovery (generalizes the webhook outbox pattern) | L | **done** — `tests/test_job_queue.py::test_two_workers_claim_exactly_once`, `::test_stale_claim_is_recovered_and_reprocessed`, `::test_stale_claim_on_final_attempt_goes_dead`, `::test_transient_failures_retry_until_success`, `::test_exhausted_attempts_mark_job_dead_with_last_error`, `::test_workspace_build_completes_through_job_queue`, `::test_worker_batch_processes_queued_jobs` |
| G32 | Server-sent events for live build progress and notifications (polling kept as fallback) | M | **done** — `tests/test_sse.py::test_build_events_stream_yields_ready_frame`, `::test_build_events_cross_org_is_404`, `::test_iter_build_events_emits_each_transition_once_until_ready`, `::test_iter_build_events_times_out_without_hanging` |
| G33 | In-app notification center fed by the existing audit outbox | M | **done** — `tests/test_notifications.py::test_audit_events_map_to_notifications_with_titles`, `::test_sync_is_idempotent_and_dedups_by_source_event`, `::test_mark_read_flips_read_at_and_unread_count`, `::test_notifications_are_tenant_scoped`, `::test_mark_read_cross_org_is_not_found`, `::test_notification_endpoints_via_api` |
| G34 | Full-text search across all workspace artifacts (SQLite FTS5 / Postgres tsvector behind one interface) | L | `done` — query-time UNION over the live artifact tables behind one `search_workspace` interface (SQLite tokenized-LIKE scoring / Postgres `to_tsvector`+`plainto_tsquery`+`ts_rank`); parity + isolation + ranking + dialect-branch tests (`apps/api/tests/test_search.py`: `test_indexes_and_finds_a_term_across_multiple_artifact_types`, `test_cross_workspace_isolation`, `test_exact_term_ranks_above_partial`, `test_empty_query_and_no_hits_are_handled`, `test_engine_field_reports_the_active_backend`, `test_diligence_questions_are_searchable`, `test_postgres_path_constructs_wellformed_tsvector_query`, `test_search_endpoint_contract`) |
| G35 | Observability: Prometheus `/metrics`, structured JSON logs, request-ID propagation end-to-end (web proxy → API → workers) | M | **done** — in-process metrics registry emitting Prometheus text exposition (v0.0.4), public `/metrics` route, outermost request-ID middleware (honor-or-generate `X-Request-ID`, echoed + bound into a contextvar for JSON logs), `JSON_LOGS`-toggled structured logs, worker per-job correlation ids (`tests/test_observability.py::test_metrics_endpoint_exposes_prometheus_text`, `::test_metrics_is_public_even_when_auth_required`, `::test_request_id_is_echoed_when_supplied`, `::test_request_id_is_generated_when_absent`, `::test_request_counter_increments_across_requests`, `::test_path_template_collapses_identifiers`, `::test_json_log_formatter_emits_parseable_json_with_request_id`, `::test_json_log_formatter_omits_request_id_when_unbound`) |
| G36 | Postgres CI matrix: the full backend suite runs against a Postgres service container in addition to SQLite | M | **done** — new `api-postgres` job in `.github/workflows/ci.yml` runs on the same triggers as `api`, against a `postgres:16` service container (mirrors the compose `db` service: user/password/db `deallens`, port 5432, `pg_isready` health check). It runs `alembic upgrade head` + `alembic check` on real Postgres (this is the load-bearing extra signal — it re-plays every migration on the actual dialect, catching the 63-char-identifier class of bug SQLite hides) then the full `pytest` suite. Green is required for merge. No lockfile regeneration: `psycopg`/`psycopg-binary` are already hash-pinned in `requirements.lock`, so the unchanged `--require-hashes` base install provides the driver. `tests/conftest.py` now honors a pre-set `DEALLENS_TEST_DATABASE_URL` (else defaults to the throwaway SQLite file), so the identical suite targets Postgres in CI while the local/SQLite default stays 100% intact. Plain `postgres:16` (no pgvector) suffices: embeddings are a JSON column and FTS uses core `to_tsvector`/`ts_rank`. |
| G37 | Load-test harness (k6/Locust) with budgeted p95 latencies on the hot endpoints; CI perf smoke | M | **done** — committed budget file (`apps/api/perf/perf_budget.json`) + concurrent k6 load script (`apps/api/perf/k6_load_test.js`) + runbook (`apps/api/perf/README.md`); always-on CI gate is an offline in-process pytest smoke that runs each hot endpoint (`GET /api/workspaces`, `GET /api/workspaces/{id}`, `POST …/qa`, `GET …/search`, `POST …/underwriting/calculate`) 20× and asserts p95 ≤ budget, wired as a labeled "Performance smoke" step in ci.yml (`apps/api/tests/test_perf_smoke.py::test_perf_budget_is_wellformed_and_covers_the_hot_endpoints`, `::test_hot_endpoint_p95_within_budget[*]`). Observed local p95 single-digit ms vs 150–500 ms budgets (~50–100× headroom; verified non-flaky over 3 runs) |
| G38 | Scoped API keys for programmatic access + generated OpenAPI client | M | **done** — `tests\test_api_keys.py::test_create_returns_plaintext_once_and_stores_only_digest`, `::test_api_key_authenticates_scoped_to_org_and_rejects_revoked_and_expired`, `::test_scope_enforcement_matrix`, `::test_api_key_cannot_reach_other_organizations_workspace`, `::test_admin_gate_and_scope_validation_on_key_administration`, `::test_require_scope_dependency_is_unrestricted_for_non_key_principals` (client gen: `apps/api/scripts/generate_client.md`) |
| G39 | Per-organization quotas and rate limits (generalizes the demo limiter into tenant policy) | M | **done** — generalized the in-process sliding-window limiter into `_OrgQuotaLimiter` keyed by `(organization_id, bucket)` (`requests`/min, `builds`/hr; `limit=0`=unlimited), enforced in a dedicated `organization_quota_guard` middleware layered inner to the identity guard (principal-scoped, so `dlk_` API keys count toward their org); read via `GET /api/organizations/{id}/quota-usage`. Config `ORG_REQUEST_QUOTA_PER_MINUTE` (600) / `ORG_BUILD_QUOTA_PER_HOUR` (60). Tests: `tests/test_quotas.py::test_request_quota_boundary_is_per_tenant`, `::test_build_quota_boundary`, `::test_quota_usage_endpoint_reports_and_is_org_scoped`, `::test_unlimited_quota_never_throttles`, `::test_clearing_the_limiter_resets_usage` |
| G40 | Blob-storage abstraction: local disk default, S3-compatible option for data-room docs and the EDGAR cache | L | `done` — `BlobStore` interface (`put`/`get`/`exists`/`delete`) with `LocalDiskStore` (default, fully working) + `S3Store` (S3-compatible, written against a pluggable `S3ClientProtocol`); the EDGAR cache rewired through it. Backend-parity contract tests parametrized over local disk **and** an in-memory fake S3 client (`apps/api/tests/test_storage.py`: `test_put_get_roundtrip_is_byte_identical_on_every_backend`, `test_exists_and_delete_are_consistent_across_backends`, `test_delete_missing_key_is_idempotent_noop_across_backends`, `test_overwrite_replaces_bytes_across_backends`, `test_get_missing_key_raises_blob_not_found_across_backends`, `test_sanitize_key_rejects_traversal_and_absolute_paths`, `test_sanitize_key_normalizes_separators_and_accepts_safe_keys`, `test_local_disk_store_cannot_escape_root_even_via_get`, `test_local_disk_store_namespaces_keys_as_files_under_root`, `test_get_store_defaults_to_local_disk`, `test_get_store_s3_without_client_raises_clear_configuration_error`, `test_get_store_rejects_unknown_backend`, `test_edgar_cache_roundtrips_through_blob_store`, `test_edgar_cache_disabled_when_ttl_is_zero`, `test_edgar_cache_entry_past_ttl_is_ignored`, `test_edgar_cache_roundtrips_through_injected_s3_backend`) |

## Theme E — Collaboration & governance UX (10)

Rounds the product into something a team could actually run a deal in.

| ID | Capability | Effort | Acceptance evidence |
|---|---|---|---|
| G41 | Comment threads with @mentions on any governed artifact (risk, adjustment, memo section, packet) | L | **done** — general `Comment` model (`entity_type` ∈ risk/qoe_adjustment/memo/ic_packet/workspace) with threaded replies; `@mentions` resolved against org members → directed notifications via the audit outbox; viewer read-only guard; tenant isolation (`apps/api/tests/test_comments.py::test_create_comment_and_threaded_reply`, `::test_reply_to_foreign_parent_is_rejected`, `::test_mention_of_member_is_recorded_and_notifies_the_recipient`, `::test_mention_of_non_member_is_ignored`, `::test_viewer_is_denied_posting_but_can_read`, `::test_cross_org_comment_access_is_not_found`, `::test_resolve_flips_resolved_at`, `::test_comment_endpoints_via_api`) |
| G42 | "My reviews" inbox: one queue spanning QoE decisions, claim reviews, diligence responses, and IC comments awaiting the signed-in actor | M | `done` — cross-plane queue aggregation with per-plane four-eyes exclusion (`apps/api/tests/test_review_inbox.py`: `test_proposed_qoe_appears_for_a_different_actor_but_not_the_proposer`, `test_unreviewed_claim_appears_for_a_non_author_only`, `test_diligence_response_awaits_a_reviewer_other_than_the_responder`, `test_blocking_ic_comment_awaits_a_second_actor`, `test_counts_by_plane_span_all_four_planes`, `test_inbox_is_organization_scoped`, `test_missing_actor_is_rejected`) |
| G43 | Audit-log explorer UI: filter by actor/entity/date, export CSV | S | `done` — filter + formula-injection-safe CSV export (`apps/api/tests/test_audit_explorer.py`: `test_filter_by_actor_entity_and_date_narrows_results`, `test_csv_export_is_wellformed_and_formula_injection_safe`, `test_export_and_list_are_organization_scoped`) |
| G44 | Read-only tokenized share links for a frozen workspace snapshot (revocable, expiring) — lets an interviewer walk a finished deal with zero setup | M | **done** — opaque `dsh_` token stored only as a SHA-256 digest (plaintext shown once); public session-less `GET /api/shared/{token}` returns a non-confidential snapshot (safe default: workspace/target identity + risks, no financials/valuation/memo/data-room); revoked/expired → 410, unknown → 404; cross-workspace/cross-tenant isolation (`apps/api/tests/test_share_links.py::test_create_returns_plaintext_once_and_stores_only_the_digest`, `::test_resolve_valid_token_returns_bound_workspace_and_scope`, `::test_snapshot_excludes_confidential_content`, `::test_expired_token_is_gone`, `::test_revoked_token_is_gone`, `::test_unknown_or_malformed_token_is_not_found`, `::test_cross_workspace_token_reads_only_its_own_workspace`, `::test_create_on_foreign_tenant_workspace_is_not_found`, `::test_shared_public_endpoint_reads_then_404_after_revocation`) |
| G45 | Workspace export bundle: IC memo PDF + evidence appendix + hash manifest, verifiable offline against the packet verifier | M | **done** — ZIP (IC memo PDF + `evidence-appendix.csv` + `manifest.json` with per-file + `bundle_sha256`/`memo_sha256` digests) with a `verify_bundle` recompute-and-compare verifier; deterministic PDF (reportlab invariant) so `bundle_sha256` is stable for identical inputs. Tests: `tests/test_workspace_bundle.py::test_build_bundle_round_trips_valid`, `::test_verify_bundle_detects_tampering`, `::test_bundle_sha256_is_deterministic_for_identical_inputs`, `::test_export_and_verify_endpoints_contract` |
| G46 | IC meeting mode: full-screen packet presentation with inline decision capture and condition logging | M | **done** — presentation-flow component test (`apps/web/src/components/workbench/ICMeetingMode.test.tsx`) |
| G47 | Memo redlines: side-by-side diff of any two analysis runs with changed-claim highlighting | M | `done` — sentence-level added/removed/changed diff over sealed memo content with numeric-token change flagging (`apps/api/tests/test_memo_redline.py`: `test_diff_flags_added_removed_and_changed_numeric_claim`, `test_identical_runs_produce_an_empty_diff`, `test_comparing_a_run_with_itself_is_rejected`, `test_unknown_run_is_not_found`) |
| G48 | Optional OIDC SSO with role mapping (config-gated; password auth remains the default) | L | **done** — config-gated (`OIDC_ENABLED`, default off) authorization-code flow: `GET /api/auth/oidc/login` (authorize URL) + `GET /api/auth/oidc/callback` exchange the code via httpx, parse the `id_token` claims (signature verification intentionally out of scope — basic `iss`/`aud`/`exp` checks only, documented as a production JWKS requirement), find-or-create the user by verified email, map the IdP role claim through `OIDC_ROLE_MAP` (unmapped/missing → `viewer` least-privilege) and issue a revocable session; endpoints 404 when disabled (`apps/api/tests/test_oidc.py`: `test_login_url_when_enabled`, `test_endpoints_404_when_disabled`, `test_callback_creates_user_and_session_with_mapped_role`, `test_unmapped_role_falls_back_to_viewer_least_privilege`, `test_missing_role_claim_falls_back_to_viewer`, `test_existing_user_is_linked_and_membership_role_is_stable`, `test_unverified_email_is_rejected`, `test_expired_id_token_is_rejected`) |
| G49 | Fine-grained permission matrix beyond the four roles (per-capability grants, deny-by-default) | L | **done** — deny-by-default capability catalog with role defaults reproducing the coarse behavior, per-membership grant/revoke overrides (`membership_permissions` table), and a `require_capability` route dependency applied to the QoE decision (`underwriting:approve`) and IC decision (`ic:decide`) endpoints (`apps/api/tests/test_permissions.py`: `test_capability_catalog_is_the_authoritative_set`, `test_role_defaults_reproduce_the_coarse_behavior_exhaustively`, `test_unknown_role_is_denied_everything`, `test_granted_capability_beyond_role_is_allowed`, `test_revoked_capability_below_role_is_denied`, `test_deny_by_default_for_a_capability_outside_the_catalog`, `test_principal_without_resolved_capabilities_falls_back_to_role_defaults`, `test_require_capability_dependency_semantics`, `test_member_needs_underwriting_approve_capability_to_record_a_qoe_decision`, `test_unknown_capability_grant_is_rejected`) |
| G50 | Guided onboarding tour + contextual empty states across all workbenches | S | **done** — first-run tour component (`apps/web/src/components/OnboardingTour.tsx`), localStorage-persisted dismissal |

---

## Sequencing

Four sub-waves, ordered by interview value per unit of effort:

1. **Wave 4a — "Measured AI"** (G01–G05, G10, plus G31/G32 as enablers): hybrid retrieval with a
   CI-gated eval harness and auditably grounded generation. This is the strongest talking track
   for AI roles: *"I didn't just add RAG — I measured it, gated CI on it, and made generation
   provably faithful."*
2. **Wave 4b — "Analyst depth"** (G11–G20, G07): quarterly/segment/proxy/ownership data and the
   10-K drift diff. Widens the real-data moat and produces demo moments interviewers recognize.
3. **Wave 4c — "Institutional model"** (G21–G30): Monte Carlo, attribution, covenant headroom,
   driver models. The PE-domain fluency layer.
4. **Wave 4d — "Operable platform"** (G33–G40, G41–G50 as capacity allows): observability,
   Postgres matrix, perf budgets, collaboration. The senior-engineering maturity layer.

**Top 10 if only 10 get built:** G01, G03, G04, G07, G21, G22, G31, G32, G36, G44.
(G44 — shareable read-only deal links — is the single highest-leverage feature for interviews:
it turns every conversation into "here, click this link.")

## Explicit non-goals

Market prices, trading data, and paid feeds (no free source; would break keyless-by-default);
order execution or anything advice-like; multi-region HA (a portfolio demo does not need it and
claiming it would be theater).
